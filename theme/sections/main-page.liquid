<div class="min-h-screen bg-vinyl-paper py-24 px-4 sm:px-8">
  <div class="max-w-4xl mx-auto">
    <h1 class="font-serif text-5xl italic text-vinyl-blue mb-8 border-b border-vinyl-blue pb-4">{{ page.title }}</h1>
    <div class="prose prose-lg max-w-none font-sans text-gray-700">
      {{ page.content }}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Page",
  "settings": []
}
{% endschema %}
